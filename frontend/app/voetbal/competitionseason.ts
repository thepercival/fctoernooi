import {VoetbalInterface} from './interface';

export class CompetitionSeason implements VoetbalInterface{
    id: number;
    name: string;
    seasonName: string;
    // teams: Team[];
    // rounds: Round[];
}